machine:
  node:
    version: 6.10.3
  python:
    version: 3.6.3

dependencies:
  pre:
    - pip install -r requirements_for_test.txt

test:
  override:
    - make test

deployment:
  staging:
    branch: master
    heroku:
      appname: dit-help-staging
