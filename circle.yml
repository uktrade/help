machine:
  node:
    version: 6.10.3
  python:
    version: 3.6.3

dependencies:
  pre:
    - pip install -r requirements_for_test.txt
    - pip install codecov

test:
  override:
    - make test
  post:
    - codecov
